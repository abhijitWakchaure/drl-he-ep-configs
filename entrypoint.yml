version:
  config: "1.0.0"
  service: "0.0.1-SNAPSHOT"

server:
  port: 8080

springdoc:
  swagger-ui:
    enabled: true
    path: /swagger-ui
  api-docs:
    enabled: true
    path: /swagger

management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - refresh
